# Background Verification AI API

> **Deprecated.** Use the unified service at [`apps/ai-service`](../ai-service) (port **8001**). This folder is kept only for reference.

Previously ran on host port **8004**. All BGV routes now live on the shared AI service:

`POST /api/v1/ai/background/analyze`
