# BesTal Web — environment variables
# Prefer relative /api/v1 so Vite proxies to the API (works with port-forwarded 5173).
# For direct browser→API calls: VITE_API_BASE_URL=http://localhost:3001/api/v1
VITE_API_BASE_URL=/api/v1
